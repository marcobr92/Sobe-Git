# Sobe-Git

A minimal Git learning / practice repo (Portuguese-speaking author). No application code, no build system, no tests.

## Active branch

The current working branch is `atualiza-pipe`. All new work should be based there unless instructed otherwise.

## CI

- `.github/workflows/blank.yml` — basic hello-world workflow on push/PR to `main` only.
- No additional test/lint/deploy steps configured.

## Conventions

- Branch names and commit messages are in Brazilian Portuguese (e.g. `atualiza-branch`, `feature/readme-upd`).
- PRs are merged via merge commits (not squash or rebase).
- Existing branches: `main`, `atualiza-pipe`, `atualiza-branch`, `feature/readme-upd`, `feature/updt-actions`.

## Commands

No standard dev commands exist (`npm`, `pip`, etc. not used). This repo is tested by pushing to GitHub and verifying CI.

## Ownership

- Remote: `marcobr92/Sobe-Git` on GitHub.
